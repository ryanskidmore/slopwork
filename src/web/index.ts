/**
 * Read-only local web explorer (`slop web`).
 *
 * Landing here in work item D5: ticket list/filters, tree view (external
 * parents as badges), ticket detail (spec/timeline/sessions/plans),
 * transcript viewer, review panel, stale/resumable panel. See design.md
 * §4.4. Read-only for v0 — mutations arrive with F9.
 *
 * A1 only establishes the module — nothing here yet.
 */
export {};
